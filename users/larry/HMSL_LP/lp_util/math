\ Math tools.\ fixed pointANEW TASK-MATH: 3**N ( N -- 3**N )    1 swap 0    DO 3 * LOOP;: X**Y ( X Y -- X**Y )    >r 1 r> 0    DO over *    LOOP nip;: FACTORIAL  ( N -- N! )    1 swap 1+ 1 DO i * LOOP;: TAKEN  ( N R -- N_taken_R_at_a_time )    over factorial    over factorial    2swap - factorial * /;: (-1)J ( J -- +1|-1)    1 and IF -1 ELSE 1 THEN;: N.OF.NK { NVAL KVAL | ACCUM -- , }    0 -> accum    kval 1+ 0    DO i (-1)J       kval i taken *       kval i - nval x**y *       accum + -> accum    LOOP    accum;: NUM.ORDERS  { NVAL | ACCUM -- , num ways to put N objects in K places w/ = }    0 -> accum    nval 1+ 1    DO  nval i n.of.nk        accum + -> accum    LOOP    accum;: ISODD? 	2 mod 0= not;: EVEN-DOWN 	dup isodd? IF 1- THEN;\ this is a very funky and slow normal distribution generator...: GAUSSIAN { mean range | upper lower ave -- gaussian-value }	mean range + -> upper	mean range - -> lower	0 -> ave	3 0 DO		upper lower wchoose		ave + -> ave	LOOP	ave 3 / ;